// Problem: Check if both numbers are odd.
// If true, print "we are odd", otherwise print "We are simple".

let a = 1;
let b = 3;

if (a % 2 !== 0 && b % 2 !== 0) {
    console.log("we are odd");
} else {
    console.log("We are simple");
}
